# Evaluator 项目录制脚本

参考材料：`D:\Tencent\xwechat_files\wxid_7xi8eqe5zvfq22_474b\msg\file\2026-05\视频脚本(1).pdf`

目标时长：6 分 30 秒以内

录制口径：

- 解说词绑定画面动作，例如“我现在点击”“这里选择”“接下来展开”。
- 评测配置相关概念集中在“评测配置”页面讲清楚。
- 创建任务阶段只演示选择并应用配置、补齐任务信息、启动任务，不再重复解释配置项。
- 主流程使用 `HYBRID` 从创建任务演示到评测运行。
- 结果展示只展示 `RESULT` 和 `DETERMINISTIC`，不展开 `PROCESS` 或 BT 排名。

## 录制前准备

1. 启动 `evaluator/frontend`、`evaluator/backend`、`evaluator/agent`。
2. 模型管理中准备可用参评模型和裁判模型。
3. 数据集管理中准备一个可用数据集，例如 `trip_agent_eval_demo`。
4. 评测配置中准备或现场展示一条 `RESULT + HYBRID` 配置。
5. 任务列表中准备一个已完成的 `DETERMINISTIC` 单模型任务，用于最后展示确定性结果。

## 时间轴与完整台词

| 时间 | 录屏动作 | 解说词 |
| --- | --- | --- |
| 00:00-00:20 | 打开 evaluator 首页或总览页 | 大家好，现在我打开的是我们项目中的 evaluator 评测系统。这个系统用于评测智能旅行 Agent 或大模型回答质量。本次我会先介绍项目架构和 agent 工具链，然后重点在评测配置页面讲清楚配置含义，最后用 HYBRID 创建并运行一次评测任务。 |
| 00:20-00:50 | 鼠标指向左侧导航和页面主体 | 我现在先介绍整体架构。项目分为三层：前端 `evaluator/frontend` 负责页面交互；后端 `evaluator/backend` 负责接口、任务调度、评测执行、指标计算和结果落库；agent 端 `evaluator/agent` 负责对话接口和 Ragas judge 评分接口。整体流程是前端收集配置，后端执行评测，agent 和模型服务提供回答生成与评分能力。 |
| 00:50-01:10 | 切到 agent 目录或项目说明页，指向 `requirements.txt`、`app/main.py` | 这里我简要介绍 agent 端工具链。agent 服务用 `FastAPI` 提供接口，用 `Uvicorn` 启动；`Pydantic` 做参数校验；`StreamingResponse` 返回 SSE 流式回答；Ragas 评分接口会通过 `LangChain` 和 OpenAI 兼容 SDK 调用 ModelScope 兼容模型。 |
| 01:10-01:30 | 点击“模型管理” | 我现在点击模型管理。这里保存评测会用到的大模型配置，包括模型名称、供应商、接口地址、角色和启用状态。后续评测中的参评模型负责生成回答，裁判模型负责 judge 判断。 |
| 01:30-01:50 | 点击“数据集管理” | 接下来我点击数据集管理。评测任务基于数据集批量运行，每条样本一般包含输入问题和期望输出。任务启动后，后端会逐条读取样本，调用模型或 agent 生成回答，再计算每条样本得分。 |
| 01:50-02:10 | 点击“评测配置” | 现在我进入评测配置页面。这里是本次要重点说明的部分。我们已经取消旧的配置版本概念，所以每一条评测配置就是一个可直接复用的最终配置，创建任务时可以一键应用。 |
| 02:10-02:45 | 在评测配置页指向配置名称、评测模式、评测方法 | 我现在先看基础配置。评测名称用于标识这套配置。评测模式决定系统看什么：`RESULT` 只看输入和最终输出，适合展示最终回答质量；`PROCESS` 会额外看工具轨迹，适合评估 Agent 过程是否合理。本次录制只展示 `RESULT`。评测方法决定系统怎么打分：`DETERMINISTIC` 是规则匹配，`JUDGE` 是模型裁判，`HYBRID` 是规则分和 judge 分的组合。 |
| 02:45-03:25 | 在评测方法或模型选择区域补充指向 judge / BT 相关配置 | 这里我补充一下 judge 评测里的 BT 多模型方式。普通 judge 是让裁判模型给单个回答打分；BT，也就是 Bradley-Terry 模型，则是让多个参评模型先分别回答，再由裁判模型对回答做两两比较。它的基本思想是：每个模型背后都有一个潜在能力值，两个模型对比时，能力值越高的一方胜出的概率越大。系统收集大量 A 胜 B、B 胜 C 这类成对结果后，就可以反推出每个模型的能力值，再转换成排行榜。因此 BT 更适合回答“多个模型谁整体更强”这个问题。本次演示只介绍原理，不展开 BT 结果页。 |
| 03:25-03:45 | 指向评测维度、指标集、自定义指标区域 | 接下来这些杂项配置我简单带过。评测维度包括效果、安全和性能；指标集用于补充关键词、结构或业务规则；这些内容会跟随配置保存，保证不同任务使用同一套评测标准。 |
| 03:45-04:05 | 指向模型配置、推理参数、滑块或参数输入区 | 这里是模型和推理参数，比如 temperature、maxTokens、timeout 和重试次数。它们分别控制输出随机性、最大输出长度和运行稳定性。保存后，创建任务时可以直接复用。 |
| 04:05-04:20 | 点击保存或指向“一键应用”相关提示 | 到这里，一条评测配置就包含了模式、方法、维度、指标、模型和推理参数。保存后，它在创建任务页面可以一键应用。后面我创建任务时只演示应用这条配置，不再重复解释这些字段。 |
| 04:20-04:35 | 点击“评测任务”，进入任务列表 | 现在我点击评测任务，进入任务列表。这里能看到所有任务的状态、数据集、样本数和运行结果。接下来我点击创建任务，演示一次完整 HYBRID 评测。 |
| 04:35-04:55 | 点击“创建任务”，第一步选择 `HYBRID` | 现在弹出创建任务窗口。第一步我选择 HYBRID，表示这次任务会使用混合评测。具体的配置含义刚才已经在评测配置页面讲过，这里只做任务创建操作。 |
| 04:55-05:10 | 点击“下一步”，进入第二步配置页 | 我现在点击下一步。第二步是任务配置页，这里主要补齐任务名称、应用本地评测配置、选择数据集，然后启动任务。 |
| 05:10-05:30 | 在“本地评测配置”下拉框选择配置，点击“一键应用” | 我现在选择刚才说明的本地评测配置，然后点击一键应用。点击后，系统会自动带入模式、方法、维度、模型、指标和推理参数。这里创建任务就不需要重新配置评测逻辑。 |
| 05:30-05:45 | 填写评测名称，选择数据源 | 接下来我填写评测名称，并选择本次要跑的数据集。数据集确定了这次评测要执行哪些样本，配置确定了这些样本如何被打分。 |
| 05:45-06:00 | 点击“创建并启动”，等待跳转运行详情或监控页 | 配置完成后，我点击创建并启动。前端会先创建任务，再启动运行。后端随后异步执行评测：加载数据集，调用模型或 agent，收集回答，执行 HYBRID 打分，最后保存 QA 记录和指标快照。 |
| 06:00-06:15 | 在运行详情页指向状态、指标卡片、样本表格 | 现在进入运行结果页。这里我只展示 `RESULT` 模式结果，也就是最终回答质量。页面中可以看到任务状态、样本数量、通过率、总分，以及每条样本的输入、期望输出和实际输出。 |
| 06:15-06:25 | 展示本次 HYBRID 的 RESULT 样本结果 | 这里重点看样本结果。HYBRID 最终分综合规则命中情况和 judge 语义判断，因此比纯规则评测更灵活，也比纯 judge 更稳定。 |
| 06:25-06:30 | 快速切到已完成的 `DETERMINISTIC` 任务结果 | 最后我快速打开一个 `DETERMINISTIC` 任务结果。这个模式只做规则匹配，结果稳定、可复现、成本低。到这里，本次演示完成：配置含义在评测配置页讲清楚，创建任务只负责应用配置并启动运行。 |

## 录制重点

1. 评测配置页面是解释重点，必须讲清：评测模式、评测方法、评测维度、指标集、模型和推理参数。
2. 创建任务页面不要重复解释配置项，只讲选择 `HYBRID`、一键应用本地配置、填写名称、选择数据集、创建并启动。
3. Agent 端工具链压缩讲：`FastAPI`、`Uvicorn`、`Pydantic`、`StreamingResponse/SSE`、`LangChain`、OpenAI 兼容 SDK、`Ragas`。
4. Judge 模式说明中要补充 BT 原理：多个模型回答、裁判两两比较、模型潜在能力值、由胜负关系拟合排名。
5. 结果展示只讲 `RESULT` 和 `DETERMINISTIC`，不展开 `PROCESS` 或 BT 排名。
6. 口播要带动作，不要变成纯概念说明。
