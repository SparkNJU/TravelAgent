# 迭代三汇报 PPT 大纲

**项目**: TravelMind — 智能旅游规划助手
**日期**: 2026-06-15
**两条主线**: Agent 产品化 + Agent 能力优化

---

## Slide 1: 封面

- 标题: TravelMind — 智能旅游规划助手
- 副标题: 迭代三成果汇报
- 团队: 软件工程与计算3 · 02 组
- 日期: 2026 年 6 月

---

## Slide 2: 产品背景 — 我们在做什么？

**核心定位**: 将生活内容社区 + 本地生活服务融为一体

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│   小红书风格社区        +      高德/美团本地服务     │
│   (UGC 内容分享)              (吃喝玩乐即时搜索)    │
│                                                     │
│              = 人人都是内容创作者                     │
│              = 人人可定制自己的旅行生活方式            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

- 用户通过 AI 对话快速生成个性化旅行规划
- 规划可发布到社区，其他用户可直接导入并修改复用
- 跨对话的知识共享让 Agent 越用越懂你

---

## Slide 3: 两条主线总览

```
┌──────────────────────────────┬──────────────────────────────┐
│     主线一: Agent 产品化      │     主线二: Agent 能力优化     │
│                              │                              │
│  · Skill 技能管理系统         │  · PlanChecker 结构验证       │
│  · 长期记忆 + 用户画像        │  · AI 随机性四层防御体系       │
│  · RAG 知识库管理             │  · TravelPlanner Benchmark   │
│  · 高德地图工作台             │  · 工具链重构与扩展           │
│  · 模型竞技场                 │  · 上下文压缩工程             │
│  · 社区分享与复用             │  · RAG 知识增强               │
│                              │                              │
│  目标: 从"能用"到"好用"       │  目标: 从"能跑"到"跑得好"     │
└──────────────────────────────┴──────────────────────────────┘
```

---

# 主线一: Agent 产品化

---

## Slide 4: 产品化 — Skill 技能管理系统

**基于 Anthropic Skills 范式的三级渐进式披露**

```
Level 1: 元数据常驻
  Agent 启动时，仅将技能 name + description 注入 System Prompt
      ↓ LLM 判断匹配
Level 2: 按需动态激活
  调用 activate_skill 工具，加载完整 SKILL.md 指令到上下文
      ↓ 需要执行
Level 3: 精准工具调用
  技能注册的 Python 脚本作为独立 Tool 被调用
```

**内置技能**:
| 技能 | 触发场景 | 核心能力 |
|------|----------|----------|
| `budget-optimizer` | "省钱"、"预算有限" | 3:2:3:2 消费分配法则 |
| `packing-helper` | "带什么"、"行李清单" | 天气+天数+同行人生成清单 |
| `skill-creator` | "创建新技能" | 对话式引导用户定义技能 |

**⭐ 加分亮点**: 用户可在设置页自定义创建/编辑/删除/启禁用技能，Agent 按需动态激活

---

## Slide 5: 产品化 — 跨对话知识共享

**三大知识载体，让 Agent 越用越懂你**

```
┌──────────────────────────────────────────────┐
│  长期记忆（User Agent Memory）                │
│  · 每次对话后自动提取用户偏好                   │
│  · "用户有一个 5 岁孩子"                       │
│  · "偏好经济型酒店，人均 200 以内"              │
│  · 下次对话自动注入 System Prompt              │
├──────────────────────────────────────────────┤
│  RAG 知识库（Milvus 向量检索）                 │
│  · 对话结果 + 联网搜索 → 向量化存储            │
│  · 稠密(75%) + BM25 稀疏(25%) 混合检索       │
│  · knowledge_search 工具按需检索               │
├──────────────────────────────────────────────┤
│  用户偏好卡片（Agent Memories）                │
│  · 用户手动创建/编辑/删除                       │
│  · 设置页可视化管理                            │
│  · 支持启用/禁用，控制注入上下文                 │
└──────────────────────────────────────────────┘
```

**⭐ 加分亮点**: 跨对话知识共享 — 用户无需反复输入个人信息，Agent 自动识别并记忆偏好

---

## Slide 6: 产品化 — 高德地图集成与规划工作台

**全屏地图 + 日轴 + 右侧编辑，沉浸式行程管理**

```
┌──────────────────────────────────────────────────────┐
│ [← 返回对话]  东京一日游        [保存]  [AI 微调]     │
├──────────────────────────────────────────────────────┤
│ 日轴: [ D1 ● ] — [ D2 ○ ] — [ D3 ○ ] — [ ＋ ]      │
├─────────────────────────────────┬────────────────────┤
│                                 │  右侧编辑区        │
│    🔍 搜索地点                  │  ┌ 第 2 天 ─────┐ │
│                                 │  │ + 添加条目    │ │
│    全屏地图 + 标记              │  │ 09:00 涩谷    │ │
│    · 选中日高亮                 │  │ [重选位置]    │ │
│    · 标点模式交互               │  │ 描述、费用... │ │
│                                 │  └──────────────┘ │
└─────────────────────────────────┴────────────────────┘
```

**⭐ 加分亮点**: 三级坐标查找（本地地标库→高德 API→城市中心兜底），编辑自动同步，对话↔工作台往返不丢进度

---

## Slide 7: 产品化 — 社区分享与复用 + 模型竞技场

### 社区分享闭环

```
对话生成规划 → 分享到社区论坛 → 他人浏览 → 一键导入到对话 → AI 微调修改
```

- 小红书风格社区，用户可发布/浏览/点赞旅行规划
- 规划可直接导入到 AI 对话进行二次修改

### 模型竞技场

```
用户发起对比 → 随机选 2 个模型并行调用 → 前端并排 SSE 流式展示 → 投票 → Bradley-Terry 排行榜
```

**⭐ 加分亮点**: 多模型实时对比评测，基于 Bradley-Terry 算法的 Elo 排行榜

---

# 主线二: Agent 能力优化

---

## Slide 8: 能力优化 — PlanChecker 子 Agent（核心突破）

**在 Agent 输出前插入结构验证 + Reflexion 自我修正**

```
ReAct Agent 调用 finish(answer)
        │
        ▼
  PlanChecker 检查 8 条结构规则
        │
   ┌────┴────┐zai
   │         │
合规      不合规
   │         │
   ▼         ▼
输出答案   格式化违规反馈
             │
             ▼
   注入 tool result → Agent 自主修正
   → 再次 finish（最多 2 轮）
```

**8 条结构规则**: 闭环行程 · 餐厅不重复 · 景点不重复 · 交通一致性 · 住宿合理性 · 天数覆盖 · 城市覆盖 · 餐饮完整

---

## Slide 9: 能力优化 — TravelPlanner Benchmark 评测

**基于 ICML'24 TravelPlanner 数据集的系统性评估与优化**

### 优化前后对比

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| 最终通过率 | **10%** | **30%** | **3 倍提升** |
| 解析成功率 | 80% | **100%** | +20% |
| 结构违规率 | 30% | **0%** | **完全消除** |
| 常识约束通过率 | 10% | **40%** | +30% |

### Bad Case 驱动优化

| Bad Case | 问题 | 优化手段 | 效果 |
|----------|------|----------|------|
| idx 1 | 餐厅重复使用 | PlanChecker + Reflexion | ✅ 修复 |
| idx 8 | 行程非闭环 + 住宿天数不足 | PlanChecker + 补充返程 | ✅ 修复 |
| idx 9 | 交通方式冲突 | PlanChecker 统一交通 | ✅ 修复 |
| idx 6 | 5 天行程解析失败 | Plan Parser 格式约束加强 | ✅ 修复 |

---

## Slide 10: 能力优化 — AI 随机性四层防御体系

**核心理念: 不消除随机性，而是通过工程手段将其约束在可控范围内**

```
┌───────────────────────────────────────────────────┐
│  Layer 4: 输出后验证                                │
│  PlanChecker 结构检查 · Reflection 质量评估         │
├───────────────────────────────────────────────────┤
│  Layer 3: 输出格式约束                              │
│  JSON Schema 强制 · 输出模板 · 空答案检测           │
├───────────────────────────────────────────────────┤
│  Layer 2: 推理过程控制                              │
│  温度分级（主对话 0.7 / 结构化 0.1）                │
│  工具调用自纠正 · 上下文压缩                        │
├───────────────────────────────────────────────────┤
│  Layer 1: 输入标准化                                │
│  Arena 模式约束 · 每请求隔离 · 配置三层覆盖          │
└───────────────────────────────────────────────────┘
```

---

## Slide 11: 能力优化 — 工具链重构与扩展

**核心思路**: 抽象基类统一接口 + LLM 自纠正重试 + 多级降级兜底

**代码片段 1 — Tool 抽象基类 + 注册表**

```python
# agent/services/tool_registry.py
class Tool(ABC):
    @property
    @abstractmethod
    def name(self) -> str: ...
    @property
    @abstractmethod
    def parameters_schema(self) -> dict: ...
    @abstractmethod
    def execute(self, **kwargs) -> Any: ...

class ToolRegistry:
    def register(self, tool: Tool) -> None:
        self._tools[tool.name] = tool
    def call(self, name, arguments):
        return self._tools.get(name).execute(**arguments)
```

> 所有工具实现统一接口，`build_tool_registry` 按请求动态注册，竞技场模式可禁用 ask_user / suggest

**代码片段 2 — LLM 自纠正重试**

```python
# agent/services/react_agent.py
def _execute_with_retry(self, tool_name, arguments):
    for attempt in range(self._max_retries + 1):  # 默认 2 次重试
        try:
            return self._tools.call(tool_name, arguments)
        except Exception as e:
            if attempt < self._max_retries:
                fixed = self._self_correct(tool_name, arguments, str(e))
                if fixed:
                    arguments = fixed  # LLM 修正参数后重试
    return {"error": f"Tool {tool_name} failed after retries"}
```

> 工具调用失败不直接报错，把错误信息发给 LLM 修正 JSON 参数，最多重试 2 次

**其他优化**:
- 高德坐标三级兜底：本地地标库 → 高德 API → 城市中心偏移
- Plan Parser 双路径：`chat_json` → `chat` + 正则 → 硬编码空结构
- 所有 HTTP 调用设超时（搜索 20s、地理编码 10s、技能激活 5s）

---

## Slide 12: 能力优化 — 上下文压缩工程

**核心问题**: 多轮对话 + 工具调用积累大量历史，Token 占用逼近模型上限

**压缩策略**:

```
┌─────────────────────────────────────────────────┐
│  系统消息 (System Prompt)          ← 始终保留    │
├─────────────────────────────────────────────────┤
│  压缩摘要 (LLM 生成)              ← 旧对话压缩  │
├─────────────────────────────────────────────────┤
│  最近 2 轮对话                    ← 原文保留     │
└─────────────────────────────────────────────────┘
```

- **触发条件**: Token 占用超过阈值（默认 80%），每次新消息到达时检测
- **压缩方式**: 用低温度 LLM（temperature=0.1）将旧对话摘要为忠实总结
- **保留策略**: 系统消息 + 最近 2 轮原文不动，只压缩更早的历史
- **为什么用 LLM 而不是截断**: 截断会丢失用户偏好和关键决策，摘要保留语义
- **通知机制**: 压缩后通过 SSE 通知前端，用户可感知上下文已被压缩
- **可配置参数**: `compress_threshold`（阈值）、`compress_keep_last`（保留轮数）

---

## Slide 13: 能力优化 — RAG 知识增强

**核心思路**: 对话结果 + 联网搜索 → 向量化存储 → 混合检索，让 Agent 规划有据可依

**存储架构**:

```
用户对话 + 联网搜索结果
        │
        ▼
  Chunking → Embedding → Milvus 向量库
                           │
                    ┌──────┴──────┐
                    │  稠密 75%   │ 语义相似度 (COSINE, HNSW)
                    │  +          │
                    │  BM25 25%  │ 关键词精确匹配
                    └──────┬──────┘
                           │
                    WeightedRanker 加权融合
                           │
                    Agent 通过 knowledge_search 工具按需检索
```

**关键技术点**:

- **混合检索**: 稠密向量（语义匹配）+ BM25 稀疏（关键词匹配）加权融合，兼顾"意思相近"和"字面命中"
- **中文分词**: Milvus Schema 启用 `analyzer: chinese`，BM25 索引自动构建
- **Embedding 缓存**: SHA-256 哈希做 key 的 write-through 缓存，相同文本只调用一次 API
- **命名空间隔离**: 按用户 namespace 隔离检索范围，不同用户知识库互不干扰
- **两种接入模式**: `inprocess`（进程内直连）/ `http`（远程服务），外部服务不可用时可降级

---

## Slide 14: 实机演示 — Demo 脚本

### Demo 1: AI 对话生成旅行规划（2-3 分钟）

1. 输入: *"帮我规划一个三亚 5 天亲子游，预算 5000，一家三口，孩子 5 岁"*
2. 展示 Agent 多轮对话过程（Think → Act → Observe 实时展示）
3. 展示联网搜索过程
4. PlanChecker 自动检查 → 输出最终规划

### Demo 2: 规划工作台可视化（1-2 分钟）

1. 点击"进入工作台" → 全屏地图 + 日轴
2. 点击日轴 D2 → 右侧编辑区展示条目
3. 演示"重选位置"标点模式

### Demo 3: 知识库 + 记忆 + 技能管理（1-2 分钟）

1. 设置页 → 技能管理（查看/创建自定义技能）
2. 记忆管理（查看自动提取的偏好 + 手动添加）
3. 知识库（查看已同步文档 + 上传新文档）
4. 新开对话，验证 Agent 已记住用户偏好

### Demo 4: 社区分享与复用（1 分钟）

1. 将规划发布到社区
2. 浏览他人规划 → 一键导入到对话 → AI 微调

---

## Slide 15: 技术架构总览

```
Frontend (Vue 3 + Vite, :5173)
  │  /api/* (Vite proxy)
Backend (Spring Boot 4, :8080)
  │  HTTP to :8000
Agent (FastAPI, :8000)
  │  HTTPS
  ├── Milvus (:19530) ← RAG 向量知识库
  └── DashScope API ← Embedding + LLM
```

| 模块 | 技术 | 主线归属 |
|------|------|----------|
| Skill 技能管理 | 三级渐进式披露 | 产品化 |
| 长期记忆 | LLM 提取 + 双层存储 + 审计日志 | 产品化 |
| 规划工作台 | 高德 API + 三级坐标查找 | 产品化 |
| 社区分享 | Spring Boot + Vue 3 | 产品化 |
| PlanChecker | LLM 检查 + Reflexion 修正 | 能力优化 |
| 四层防御 | 温度分级 + Schema + 自纠正 + 验证 | 能力优化 |
| Benchmark | TravelPlanner 评测框架 | 能力优化 |
| 工具链 | 重试 + 降级 + 自纠正 | 能力优化 |
| 上下文压缩 | 自动摘要 + 保留最近 N 轮 | 能力优化 |
| RAG 知识增强 | Milvus 混合检索 | 能力优化 |

---

## Slide 16: 团队分工

| 成员 | 职责 | 主线 | 核心交付 |
|------|------|------|----------|
| 贾亦宸 | Skill 管理 | 产品化 | 技能 CRUD、三级渐进式披露、skill-creator |
| 李苑平 | 记忆与上下文 | 产品化+优化 | 长期记忆、用户画像、上下文压缩、审计日志 |
| 周林辉 | 本地服务接入 | 产品化 | 高德 API、地理编码、规划工作台、地图组件 |
| 张耀宇 | Agent 评估与优化 | 能力优化 | Benchmark、PlanChecker、工具链重构、随机性优化 |
| 陈展 | RAG 知识库 | 产品化+优化 | Milvus 向量库、混合检索、知识同步、文档管理 |
| 谢佳轩 | 人机交互优化 | 产品化 | UI 优化、模型竞技场、前后端联调 |

---

## Slide 17: 总结与展望

### 迭代三核心成果

| 主线 | 核心成果 |
|------|----------|
| **产品化** | Skill 技能管理 + 长期记忆 + 工具开关 + 知识库管理 + 规划工作台 + 社区分享 |
| **能力优化** | PlanChecker 结构违规 30%→0% + 解析成功率 80%→100% + 四层防御体系 + Benchmark 3 倍提升 |

### 下一步方向

| 方向 | 说明 |
|------|------|
| Sandbox 覆盖 | 解决 Benchmark 71% 的 sandbox 不匹配问题 |
| 查询规范化 | 搜索关键词预处理，提升搜索结果质量 |
| Rerank 集成 | 启用 qwen3-rerank，提升知识检索精度 |
| 移动端适配 | 响应式工作台，支持手机端查看与编辑 |

---

## Slide 18: Q&A

- 感谢老师和同学们的聆听
- 欢迎提问与交流
